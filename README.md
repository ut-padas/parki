## (Par)allel (Par)ticle (K)ernel (I)nteractions library: ParKI
The ParKI library provides a python API, ParKIPy, supporting a `CellList` class for local-particle interactions and a `ewald` module for computing Ewald summations of the Stokes and Laplace kernels in arbitrary periodicities. 
